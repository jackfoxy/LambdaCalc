/* requires prelude.lmbd, boolean.lmbd, and tuple.lmbd */
nil = bottom;
isnil = λl. l (λh. λt. fls) tru;
cons = lambda h. lambda t.lambda c. c (pair h t);
head = lambda l. l fst;
tail = lambda l. l snd;
